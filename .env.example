# copy to .env and fill in. default provider is anthropic (Claude).
LLM_PROVIDER=anthropic
ANTHROPIC_API_KEY=sk-ant-...

# only needed if you run ingest/fetch_filings.py against live EDGAR
EDGAR_UA=Your Name your@email.com

# to use openai instead:
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-...
